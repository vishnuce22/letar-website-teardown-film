.fal-key
.higgsfield-key
node_modules/
# raw 1080p masters (20-40 MB each) — only the -hd.mp4 web encodes ship
assets/gen/*-final.mp4
# journey-film working takes (attempts) — only approved encodes ship
assets/journey-work/
